# ⚛️ react-useeffect-cleanup-patterns

Demonstrates correct cleanup of side effects in React’s `useEffect` hook
to prevent memory leaks, redundant timers, and stale closures.

## Run
```bash
npm install
npm run dev
